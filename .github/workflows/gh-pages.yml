name: (disabled) Deploy Flutter Web to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (disabled)
        uses: actions/checkout@v4

      - name: Setup Flutter (disabled)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: 'stable'

      - name: Install dependencies (disabled)
        run: echo "disabled"

      - name: Build web (disabled)
        run: echo "disabled"

      - name: Deploy to GitHub Pages (disabled)
        run: echo "disabled"

